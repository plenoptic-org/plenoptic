language: python
dist: xenial
services:
  - xvfb
env:
  - TEST_SCRIPT=metamers
  - TEST_SCRIPT=models
  - TEST_SCRIPT=plenoptic
  - TEST_SCRIPT=steerable_pyr
  - TEST_SCRIPT=synthesis
  - TEST_SCRIPT=mad
python:
  - "3.6"
  - "3.7"
install:
  - pip install -e .
script:
  - pytest tests/test_${TEST_SCRIPT}.py
